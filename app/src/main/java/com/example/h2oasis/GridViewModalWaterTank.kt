package com.example.h2oasis

data class GridViewModalWaterTank (
    // we are creating a modal class with 2 member
    // one is course name as string and
    // other course img as int.
    val courseId: Int,
    val courseName: String,
    val courseImg: Int
)